<form method="<?= htmlspecialchars($method) ?>" action="<?= htmlspecialchars($action) ?>">
	<?php foreach ($this->fields as $name => $field): ?>
		<div>
			<?= $field->input($name) ?>
		</div>
	<?php endforeach ?>
</form>